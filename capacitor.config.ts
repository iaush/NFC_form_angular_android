import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.nfcform.com',
  appName: 'nfc-form',
  webDir: 'dist/nfc-form',
  bundledWebRuntime: false
};

export default config;
